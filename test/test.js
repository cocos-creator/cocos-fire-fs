var FireFs = require('../index');

describe('TODO', function () {
    it('should work for simple case', function () {
        // Path.join('foo', 'bar')
        //     .should.eql('foo/bar');
    });
});
